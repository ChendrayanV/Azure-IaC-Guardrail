# Standards tests

Standards validation tests are executed from the extension's `test/unit`
suite. They verify catalog structure, versions, unique control IDs, and control
behavior against Terraform fixtures.
